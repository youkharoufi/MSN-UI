import { SearchForFriendsPipe } from './search-for-friends.pipe';

describe('SearchForFriendsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchForFriendsPipe();
    expect(pipe).toBeTruthy();
  });
});
